Demo-request card (schools / tuition centres): four Fields + ink pill submit in a white bordered card. Brand rule: never fake a wired-up form — leave `connected` false until it actually posts somewhere.

```jsx
<LeadForm orgLabel="Centre" />
```
